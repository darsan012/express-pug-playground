import { Router } from "express";

const router = Router();

// crud operations on the user data
router.get('/user',()=>{}); 
router.post('/user',()=>{});
router.put('/user/:id',()=>{});
router.delete('/user/:id',()=>{});
